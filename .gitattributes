# Shell scripts must use LF line endings so they run on Linux/macOS regardless
# of the committer's platform — a CRLF `#!/bin/sh` script fails with a confusing
# "bad interpreter" error.
*.sh    text eol=lf
*.bash  text eol=lf
*.zsh   text eol=lf
*.fish  text eol=lf
