[package]
name = "ciri-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }
