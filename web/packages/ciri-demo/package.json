{
  "name": "@ciri/demo",
  "version": "0.1.0",
  "private": true,
  "description": "Dev-time browser demo for the ciri web client. Mounts a CiriApp into an HTML page and connects to ciri-server via the @ciri/bridge WebSocket gateway.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ciri/app": "*",
    "@ciri/client": "*",
    "@ciri/codec": "*",
    "@ciri/dom": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
