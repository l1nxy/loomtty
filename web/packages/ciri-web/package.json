{
  "name": "@ciri/web",
  "version": "0.1.0",
  "private": true,
  "description": "Production browser terminal for ciritty. Served by ciri-server's [web] gateway; connects same-origin and authenticates with a token login screen.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ciri/app": "*",
    "@ciri/client": "*",
    "@ciri/codec": "*",
    "@ciri/dom": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
