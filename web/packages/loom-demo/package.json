{
  "name": "@loom/demo",
  "version": "0.1.0",
  "private": true,
  "description": "Dev-time browser demo for the loom web client. Mounts a LoomApp into an HTML page and connects to loom-server via the @loom/bridge WebSocket gateway.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@loom/app": "*",
    "@loom/client": "*",
    "@loom/codec": "*",
    "@loom/dom": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
